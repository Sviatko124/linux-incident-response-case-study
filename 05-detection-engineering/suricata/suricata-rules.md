# Suricata Rules

The rules contained in `local.rules` detect network activity observed during the simulated incident.

Covered behaviours include:

- Nmap reconnaissance
- SSH brute force
- Netcat reverse shell communication

The rules were validated against the packet captures collected during the simulated attack.